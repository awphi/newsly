{
  "name": "server",
  "version": "1.0.0",
  "description": "Server module for programming summative.",
  "main": "node server.js",
  "scripts": {
    "pretest": "eslint --fix public/js/**/*.js app/**/*.js",
    "test": "jest",
    "dev": "concurrently \"pug --watch ./views --out ./public\" \"nodemon server.js\"",
    "build": "pug ./views --out ./public --pretty"
  },
  "author": "awphi",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "eslint": "^6.8.0",
    "nodemon": "^2.0.2",
    "pug-cli": "^1.0.0-alpha6",
    "standard": "^14.3.3",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
